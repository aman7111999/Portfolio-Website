import { PortfolioAnalysisVisual } from "@/components/case/PortfolioAnalysisStory";

/**
 * Homepage/work card visual for Portfolio Analysis.
 * Uses the real product screens rather than the generated CMS cover artwork.
 */
export function PortfolioAnalysisCardVisual() {
  return <PortfolioAnalysisVisual mode="card" />;
}
